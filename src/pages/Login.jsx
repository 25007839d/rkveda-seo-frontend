import{useState}from"react";import{useLocation,useNavigate}from"react-router-dom";import{login}from"../api/authApi";import{saveSession}from"../utils/auth";export default function Login(){const n=useNavigate(),l=useLocation(),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);async function submit(e){e.preventDefault();setError("");setLoading(true);try{const d=await login(email.trim(),password);if(!d.success||!d.token)throw Error(d.message||"Login failed");saveSession(d);n(l.state?.from?.pathname||"/dashboard",{replace:true})}catch(e){setError(e.response?.data?.message||e.message||"Unable to login")}finally{setLoading(false)}}return <div className="login-page"><section className="login-hero"><div><div className="brand"><b>R</b><div><strong>RKVeda</strong><small>SEO Platform</small></div></div><h1>Understand your website. Improve your visibility.</h1><p>Audit technical SEO, discover issues and track website performance from one simple dashboard.</p></div></section><section className="login-panel"><form onSubmit={submit}><div className="mobile-brand">RKVeda SEO</div><h2>Welcome back</h2><p>Sign in to your SEO dashboard.</p>{error&&<div className="alert">{error}</div>}<label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="primary full" disabled={loading}>{loading?"Signing in...":"Sign in"}</button></form></section></div>}